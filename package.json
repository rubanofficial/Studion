{
  "name": "focusforge",
  "private": true,
  "version": "1.0.0",
  "description": "FocusForge — a personal focus operating system. Cross-device focus timer and study analytics.",
  "type": "module",
  "engines": {
    "node": ">=20.6.0"
  },
  "workspaces": [
    "shared",
    "server",
    "web"
  ],
  "scripts": {
    "dev": "concurrently -n api,web -c magenta,cyan \"npm:dev:server\" \"npm:dev:web\"",
    "dev:server": "npm --workspace server run dev",
    "dev:web": "npm --workspace web run dev",
    "build": "npm --workspace web run build",
    "start": "npm --workspace server run start",
    "seed": "npm --workspace server run seed",
    "test": "npm --workspace shared run test && npm --workspace server run test && npm --workspace web run test",
    "test:shared": "npm --workspace shared run test",
    "test:server": "npm --workspace server run test",
    "test:web": "npm --workspace web run test",
    "typecheck": "npm --workspace web run typecheck && npm --workspace server run typecheck",
    "lint": "echo \"(lint is handled by typecheck)\" && npm run typecheck"
  },
  "devDependencies": {
    "@types/node": "^26.6.2",
    "concurrently": "^9.1.2"
  }
}
